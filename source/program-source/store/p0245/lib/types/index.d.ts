/** Build-static first-party Session format migration catalog. */
/** Local required task admission runs for every current or transformed body read. */
export declare const sessionFormatCatalog: import("@deepseek-ai/dsh-session-format").SessionFormatCatalog;
export { SessionFormatUnsupportedMigrationError } from '@deepseek-ai/dsh-session-format';
