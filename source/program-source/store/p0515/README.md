# DSH SEP branding

Adds an independently rounded SEP capsule beside the existing HARNESS wordmark through the alpha.2 sidebar brand slot. It owns no user data or network connection. Unloading it restores the existing official slot occupant. The upstream titlebar and window controls retain their own layout.
