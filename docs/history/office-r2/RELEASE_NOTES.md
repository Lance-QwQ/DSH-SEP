# 发布说明

<!-- SEP_RELEASE_STATUS_START -->
分发修订 `20260921-office-r2` · 文档 `20260921-docs-r5` · **Office 转换器替换候选**。当前包使用自有适配器和官方 LibreOffice；验证与发布状态见[发布说明](RELEASE_NOTES.md)和[测试摘要](TEST_ACCEPTANCE.md)。GitHub 仓库尚未创建，未执行上传。
<!-- SEP_RELEASE_STATUS_END -->

本版用 **SEP 自有 Office 转换适配器＋官方 LibreOffice 26.8.0.3** 替换缺少可核验 Node API 源码入口的旧 Kit。匹配的四份官方源码归档已完整下载并核对 SHA-256；旧 Kit 的 404 记录属于历史包问题，不再是本版该组件的源码入口。新实现和包级结果见[Office 转换说明](OFFICE_CONVERTER.md)及[测试摘要](TEST_ACCEPTANCE.md)。

历史 `GUARDIAN_NOT_READY` 宿主启动失败仍未归因；后来追加 **10 次**启动、记忆只读检查及正常退出均未复现，不能认定已修复。历史失败和固定验收条件保留。

文档修订 `20260921-docs-r5`，适配基线 DSH `0.1.6-alpha.2`、SEP `0.1.0-alpha.15.dsh-alpha2.1`。软件包版本号没有随所有本地修订变化，识别版本必须同时看发布标识与程序图哈希。

本次文档 r5 对接软件分发 `20260921-office-r2`，保留[产品宣传介绍](PRODUCT_OVERVIEW.md)。两种新包从已验收的修复来源生成，另行同步许可、来源、文档与包级验证；本轮专门替换 Office 转换实现，没有升级 DSH 本体版本。

## 当前构建与现有 ZIP

| 对象 | 标识 | 当前用途 |
|---|---|---|
| 已部署修复构建 | `sep-alpha2-stability-20260921`，图 `ffb961…` | 记忆／治理等既有能力来源，不是本次 Office 转换测试。 |
| 历史 Only／Full 分发档案 | `20260920-r2-sanitized`，图 `e57816…` | 历史安装与脱敏证据对象；仍含下列四组旧问题。 |
| 本次 Only／Full 分发 | `20260921-office-r2` | 新目录候选，实际图及验收见下方状态。 |
| 本文档集 | `20260921-docs-r5` | 发布资料，可单独校验；不是程序包。 |

程序图与历史档案身份在 [RELEASE_MANIFEST.json](RELEASE_MANIFEST.json)。本次归档自身最终哈希以包外 `SHA256SUMS.txt` 为准，包内不嵌入本 ZIP 哈希。[LICENSE](LICENSE) 已按项目所有者确认定稿：允许个人及企业内部使用、修改、免费转发和制作衍生版；出售或商业分发须另行授权，衍生版沿用相同商业限制。文档及工作区核心源码已同步；本次新候选按文件映射同步，已部署来源图和历史安装包保留原字节，详见 [许可说明](LICENSING.md)。

## 最新修复构建的变化

| 问题 | 修复后的行为 | 仍需注意 |
|---|---|---|
| T01 更新中心遗漏、弹窗空白 | 恢复 shell 资源、官方版本查询、启动/三小时调度和全插件清单；适配 alpha 通道、协议4与实际 Node 工作器。 | 发现版本不代表已批准安装；缺受控候选、完整性或来源证明仍会阻断。 |
| T02 长路径纳管失败 | Win32 扩展路径与持有句柄访问，明确 UTF-8 输入输出；细分路径/权限错误。 | 仅已验证的本地 Windows 来源范围，保留硬链接、重解析点与身份检查。 |
| T03 桌面误停止守护恢复 | 恢复期间清除旧连接，有界等待、核验新宿主代次并重新认证、刷新页面。 | 不扩大守护重启额度，不自动重放有副作用的业务。 |
| T04 旧锁名进入备份 | 新旧 SEP 锁名同时排除。 | 旧问题不等于已经证明记忆正文泄漏。 |

配套交叉检查还修正更新器误把 Electron 当成 Node 的启动链路。没有替换为新的上游 DSH 版本。

## 继承能力

保留此前部署的四层记忆与 RAG、同项目候选记忆、上下文管理、标准文件入口保护、受管子任务、检查点、Worker、受信任评测接口、记忆设置页、后台运行及一体式 HARNESS / SEP 标识。可选外部评测需要另行配置，不能由“接口存在”推断可直接使用所有评测服务。

Only 与 Full 均设计为新目录实例安装，原官方实例和它的插件不会自动迁入。用户自行共享同一工作目录时，共享文件仍会受到双方修改。详见 [安装](INSTALL_UNINSTALL.md) 和 [兼容范围](COMPATIBILITY_KNOWN_ISSUES.md)。

## 验证与保留问题

能力来源修复图的完整回归438/438，定向修复及三轮实际桌面测试通过；日常冷切换后只读核验通过。各组测试对象不同，不能相加宣传为独立场景总数。详细范围见 [测试摘要](TEST_ACCEPTANCE.md)。

历史工作前评测异常仍未获得唯一归因；本轮未复现不等于历史异常已修复。外部模型503等供应商错误仍可能出现。上游网络查询曾短暂失败，后续查询成功，原错误原因未确定。

本次许可覆盖、两份新包、包级脱敏与验证、可追溯源码交付按下列状态记录；GitHub 仓库尚未创建，下载与反馈地址待绑定，详见 [发布前清单](PUBLICATION_CHECKLIST.md)。本说明不宣称已满足公开稳定版全部准入条件。

[返回导航](DOCS_INDEX.md)

<!-- SEP_RELEASE_BUILD_START -->
## 本次分发身份与状态

分发 `20260921-office-r2`；程序图：`a2d3c02193007261560a35a564a786145c4424f0c4a1b28f46e0f6c5a43803a8`。本轮 Office 与实际安装包的验证结果：**pass：适配器 69/69；真实格式转换三轮 18/18，加 16 项边界共 34/34；超链接保留且零自动请求；宿主服务三轮、原字体字段断言、4 项通信契约及真实桌面预览通过。最终 ZIP 实装和隐私扫描另由包外交付报告绑定。**。只有与最终归档、图和日志绑定的结果才能计为新包验收；来源回归及旧包启动复测不会自动覆盖新适配器。

源码交付名：`DSH-SEP-Source-20260921-office-r2.zip`。包括本轮自有适配器、Windows 桥接与测试源码；其余 SEP 和宿主适配源码按 `SOURCE_FILES.json` 实际清单对应，不沿用旧包的固定文件数。官方 LibreOffice 对应源码获取入口、下载哈希和验证记录随运行库交付；四个官方源码归档可单独取得。此交付不宣称整个传递原生依赖闭包均已本地重建。

Only／Full 归档的 SHA-256 以包外 `SHA256SUMS.txt` 为准。GitHub 上传未执行。`20260921-r1` 及更早归档保留原字节；历史文档原文在 `history/r1/`，其中“当前／本次”均指当时版本。
<!-- SEP_RELEASE_BUILD_END -->
