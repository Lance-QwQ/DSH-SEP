# 兼容性与已知限制

<!-- SEP_RELEASE_STATUS_START -->
分发修订 `20260921-r1` · 文档 `20260921-docs-r4` · **候选验收未全部通过，暂不可公开发布**。GitHub 仓库尚未创建；具体下载与反馈地址未绑定。最终程序图及验证见[发布说明](RELEASE_NOTES.md)。
<!-- SEP_RELEASE_STATUS_END -->

**本次候选尚不具备公开发布条件。** 桌面聚焦补跑第3轮发生 Only 宿主在就绪前 code 1 退出（GUARDIAN_NOT_READY）；原 stderr 只记录5673字节而未留正文，原因未确定。另1次及连续5次启动诊断均未复现，不能据此认定修复。公开上传还受 LibreOffice Kit Node API 首选源码访问缺口阻塞。完整结果见[测试记录](TEST_ACCEPTANCE.md)。

更新日期：2026-09-21。定位为 **Windows x64 适配版 / 公开测试候选**，不能从本页推导为全环境稳定版认证。SEP许可已确定，见[许可说明](LICENSING.md)；本次新候选按文件权属同步许可；第三方材料与最终包核验按本轮实际证据记录，见[发布检查表](PUBLICATION_CHECKLIST.md)。

## 先区分程序修订

| 对象 | DSH 本体 | 程序图 SHA256 | 当前含义 |
|---|---|---|---|
| 日常修订 `sep-alpha2-stability-20260921` | `0.1.6-alpha.2` | `ffb961a17b4202ec753e70bc5fd0e4de0ccf697df4155ee4c369d9e48be56f6c` | 四项问题修复已部署；最终完整回归 438/438，隔离真实桌面三轮通过。 |
| 历史 Only / Full `20260920-r2-sanitized` ZIP | `0.1.6-alpha.2` | `e57816d02de36eefbcc0942ef9a644ce3e3ebbab47e303db1ab0b00278667ec5` | 历史脱敏分发候选，尚未合入这四项修复，不能以日常验证替代这两份包的结果。 |

本次另行生成 `20260921-r1` Only／Full 候选，最终图与结果见发布说明。上述历史分发、日常来源和本次分发的 DSH 版本号相同，但身份及验证对象分别记录。下载和反馈时同时记录分发修订、包哈希与程序图，详见[发布说明](RELEASE_NOTES.md)。

## 安装、系统与实例共存

| 项目 | 支持或验证范围 | 边界 |
|---|---|---|
| 系统 | Windows x64，已有当前开发测试机的受控验证 | 未完成所有 Windows 版本、ARM64、组织安全策略与硬件组合矩阵；Linux / macOS 桌面不属于本版适配范围。 |
| Full | 在尚不存在的新目录建立完整实例 | 不原地覆盖官方 DSH；内部依赖绑定安装位置，不能把已安装目录随意移动当作迁移。 |
| Only | 从可核验的固定 DSH `0.1.6-alpha.2` 来源只读复制需要的本体文件，并在新目录安装 SEP 与适配文件 | 不等于任意 alpha.2 安装都匹配；布局、依赖或文件哈希不符会拒绝。原实例自装插件不会自动复制到新实例。 |
| 与官方 DSH 共存 | 本次官方合成夹具最终3轮通过；前两次缺资源及空模板规范化误报分别保留 | 官方alpha.2桌面/宿主副本＋已记录依赖池（含部分SEP适配），不是官方签名安装器；真实官方环境更新/回退/卸载仍未全部验证。 |
| 应用实例隔离 | 独立程序、home、配置、数据、身份、端口、锁、Electron userData 与所属进程 | 不是虚拟机或恶意代码安全边界。用户主动选择同一工作目录时，双方会接触同一文件。普通 Shell 仍具有当前 Windows 用户权限。 |
| 文件与 RAG | 可核验本地普通单链接来源，Windows / NTFS 相关文件治理 | 多硬链接、路径别名或无法证明本地身份的 provider 会被拒绝。网络盘、特殊文件系统不能默认视为支持。长路径修复已验证原 277 字符案例，不保证所有依赖和所有路径长度无限适用。 |

关闭窗口会隐藏并保留本实例后台；完整退出用“应用→退出”或托盘“退出 DSH SEP”。不要按 `node` / `electron` 名称批量结束进程。

## 当前功能边界

- 自动记忆使用现有四种 Agent 模式的设置项，没有单独的第五种模式。Mem0 是同项目提炼适配，未安装完整 Mem0 云存储；lossless-claw 是来源完整性和受控原文展开适配，不是完整持久化多级 LCM 引擎。
- 工作前评测是 PinchBench 思路的本地合成就绪筛查，固定三轮且每轮达到既定 0.8 门槛才放行。它不是官方总分、复杂任务质量保证或风险操作授权。历史未归因的 `PREWORK_EXECUTION_BLOCKED` 样本仍保留；后来通过只说明未复现，未证明原因已消除。
- “最多两个子任务”、最多两次返工以及统一超时、取消、记录规则限定为**经 `suite_delegate` 创建的任务**。Profile 级模型与预算拦截是独立机制。其他插件、原生递归任务与普通 Shell 不自动取得相同生命周期保证。
- Safe Change 是单个已有 UTF-8 文件的候选、静态检查、审阅与确认发布流程。不是所有文件写入口的全局拦截，也不是完整构建测试沙箱。未配置运行测试时会拒绝 `runtimeTest`，不能把语法检查当作行为测试。
- SWE-bench / SWE-ReX 及受信任评测入口默认关闭，需另行配置受控 WSL / Docker 后端。固定公开 SymPy 与 Flask 实例已有正负对照；不能扩展成任意仓库、问题类型或真实模型正例均通过。
- 日常主模型、子任务、输出、请求体和预算均有上限；上限分离修复不保证无限长任务。HTTP 超时可能意味着结果未知；不能据此盲目重放有副作用操作或释放费用预留。
- 首次私人旧库纳管仍需要确认具体目录、写者与治理范围。合成纳管通过不意味着可以把任意旧库直接挂进受控更新器。未知治理覆盖、删除依据缺失或无法证明写者退役时保持阻断。
- 自动恢复只作用于已登记实例及可验证身份。桌面等待守护恢复上限 60 秒；身份不符、守护熔断或不能证明旧所有者已退出时停止自动等待。不会恢复所有外部进程，也不会自动重放未知副作用。

## 更新可做什么，不能跳过什么

当前日常修订恢复了打开时、运行每三小时及手动官方版本查询，保留“立即更新 / 暂不更新”和全插件兼容清单。检查本身不消耗模型 token。排队安装须等待所属桌面完整退出；只隐藏窗口不满足这一条件。

报告覆盖已登记包图、额外安装包与插件实例，包含禁用项。静态依赖、来源和指纹满足不等于运行兼容；未知项仍列为“未验证”，不能把清单条数当作全部插件通过数。用户可接受列明的兼容性风险，但不能跳过：候选缺失、来源基线或完整性无法验证、插件保留失败、维护锁、治理覆盖、恢复依据和提交条件。

只有已准备并批准、与计划和健康证据绑定的本地候选才能安装。查询到新官方版本不会自动产生兼容的 SEP 候选；包名前缀也不能证明文件可覆盖。用户自装插件、配置和数据不通过静默删除或禁用来获得“兼容”。尚无任意上游版本自动适配、构建和认证服务。

## 修复与仍需区分的证据

| 问题或验证项 | 当前状态 |
|---|---|
| 更新中心缺失、空白弹窗 | 已修复并部署于 `ffb961a1…`；旧 `e57816d0…` 包仍存在该缺陷。 |
| 长路径纳管与中文编码 | 已修复；原 277 字符输入捕获三域并核验哈希。不是任意路径无限兼容证明。 |
| 桌面阻止守护重启 | 已修复；最终三轮验证新代次重连和只读记忆接口，不重放业务请求。 |
| 历史锁名进入备份 | 已修复；新旧锁名均排除。该问题本身不等于记忆正文已泄漏。 |
| 上游 HTTP 503 等错误 | 已有脱敏分类；供应商可用性不是本地修复的保证。历史缺状态的 `PROVIDER_HTTP` 不倒推为 503。 |
| 三小时、24 小时、30 天边界 | 已有确定性时钟或合成日期验证；不宣称均完成真实连续等待。 |
| 任意新版 DSH 全流程升级、任意数据结构迁移 | 未完成普遍验证；当前事务与真实 Electron→Node Worker→合成 P2 证据不能替代所有实际安装器中断。 |
| 全部模型 / 推理组合、第三方 MCP / 搜索、音视频在线理解 | 未完整验证。图像和 RAG 固定在线冒烟不覆盖所有组合。 |
| 物理断电、磁盘拔出、所有系统重启与原生组件崩溃 | 未完整验证。受控进程终止和故障注入范围有限。 |
| 当前修复来源重新生成 Only / Full 并复验 | 本次 `20260921-r1` 的实际状态见测试摘要；历史包不改字节。 |

更多验证口径见[测试与验收摘要](TEST_ACCEPTANCE.md)。发现未覆盖环境的问题请按[反馈模板](SUPPORT.md)提供可复现的合成信息。

[返回文档导航](DOCS_INDEX.md)
